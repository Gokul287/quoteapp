import React from "react";
import ReactDOM from "react-dom/client"; // Note the correct import
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root")); // Use createRoot
root.render(<App />);
